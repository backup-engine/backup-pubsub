{
  "name": "backup-pubsub",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/service-bus": "^7.9.0",
    "@backup-engine/backup-core": "1.0.4",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/dotenv": "^8.2.0",
    "@types/node": "^16.x",
    "typescript": "^4.0.0"
  }
}
